# Neon Postgres pooled connection string (from the Neon dashboard / integration).
# Only the /health/db check uses it for now.
DATABASE_URL=

# Local dev port for the API (frontend proxies /api/geocode/* here).
PORT=8787
